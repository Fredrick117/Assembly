using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FeedbackPanel : MonoBehaviour
{
    // 1. Get ship stats of last submission
    // 2. Get last request data
    // 3. Compare request data to ship
    // 4. For each discrepancy, add a text box that describes the error
}
